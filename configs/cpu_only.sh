#!/bin/bash
# =============================================================================
# CPU only モード設定
# GPU を使わず CPU のみで推論する。GPU なし環境や省電力運用向け。
# 速度は GPU より遅いが、VRAM 制限なしに大きいモデルも動かせる。
# =============================================================================

# -----------------------------------------------------------------------------
# モデル（必須）
# -----------------------------------------------------------------------------
MODEL_PATH="models/Qwen3-VL-8B-Instruct-GGUF/Qwen3-VL-8B-Instruct-Q4_K_M.gguf"
MMPROJ_PATH="models/Qwen3-VL-8B-Instruct-GGUF/mmproj-Qwen3-VL-8B-Instruct-F16.gguf"
# ALIAS=""

# -----------------------------------------------------------------------------
# GPU 設定
# -----------------------------------------------------------------------------
N_GPU_LAYERS=0                    # 0 = 全層を CPU で処理

# -----------------------------------------------------------------------------
# CPU スレッド（CPU only では最重要パラメータ）
# -----------------------------------------------------------------------------
# THREADS=-1                      # 生成時スレッド数（-1 = 自動。論理コア数を目安に調整）
# THREADS_BATCH=-1                # バッチ処理スレッド数（-1 = THREADS と同じ）
# CPU_MASK=""                     # 使用する CPU コアのマスク（16進数）
# NUMA=""                         # NUMA 最適化: distribute / isolate / numactl

# -----------------------------------------------------------------------------
# メモリ【重要】
# CTX_SIZE と PARALLEL は CPU only では必ず明示指定すること。
# 未指定の場合、KV キャッシュがモデルのデフォルトコンテキスト長で確保され
# 数十 GB の RAM を消費してサーバーが応答不能になる。
# -----------------------------------------------------------------------------
# MLOCK=false                     # true にするとモデルを RAM に固定（スワップ防止、推奨）
CTX_SIZE=8192                     # 必須：指定しないと数十GB のKVキャッシュが確保される
# BATCH_SIZE=2048
# UBATCH_SIZE=512
# CACHE_TYPE_K="f16"
# CACHE_TYPE_V="f16"

# -----------------------------------------------------------------------------
# 並列・スループット
# -----------------------------------------------------------------------------
PARALLEL=1                        # 必須：auto（-1）は CPU only では正しく機能しない
# CONT_BATCHING=true

# -----------------------------------------------------------------------------
# サーバー
# -----------------------------------------------------------------------------
HOST="0.0.0.0"
PORT=8080
# API_KEY=""
# TIMEOUT=600
# THREADS_HTTP=-1

# -----------------------------------------------------------------------------
# 思考モード
# -----------------------------------------------------------------------------
# REASONING_FORMAT="auto"
# REASONING_BUDGET=-1

# -----------------------------------------------------------------------------
# プロセス優先度
# -----------------------------------------------------------------------------
# PRIO=0

# -----------------------------------------------------------------------------
# ログ
# -----------------------------------------------------------------------------
# LOG_VERBOSITY=3
