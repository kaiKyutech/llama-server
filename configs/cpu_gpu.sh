#!/bin/bash
# =============================================================================
# CPU + GPU 混合モード設定
# モデルの一部を GPU (VRAM) に、残りを CPU (RAM) に分散して推論する。
# VRAM に収まらない大型モデル（27B 等）を動かす際に使用。
# N_GPU_LAYERS の値で VRAM/RAM のバランスを調整する。
# =============================================================================

# -----------------------------------------------------------------------------
# モデル（必須）
# -----------------------------------------------------------------------------
MODEL_PATH="models/Qwen3-VL-8B-Instruct-GGUF/Qwen3-VL-8B-Instruct-Q4_K_M.gguf"
MMPROJ_PATH="models/Qwen3-VL-8B-Instruct-GGUF/mmproj-Qwen3-VL-8B-Instruct-F16.gguf"
# ALIAS=""

# -----------------------------------------------------------------------------
# GPU / CPU バランス（最重要パラメータ）
# -----------------------------------------------------------------------------
N_GPU_LAYERS=20                   # GPU に載せる層数。増やすほど速いが VRAM を消費する。
                                  # 起動ログの "n_layer = N" を確認して上限を把握すること。
                                  # 例: 27B (62層) で VRAM 12GB なら 30〜40 程度が目安。

# -----------------------------------------------------------------------------
# GPU 設定
# -----------------------------------------------------------------------------
# FLASH_ATTN="auto"

# -----------------------------------------------------------------------------
# CPU スレッド
# -----------------------------------------------------------------------------
# THREADS=-1                      # CPU 側の処理スレッド数
# THREADS_BATCH=-1
# NUMA=""

# -----------------------------------------------------------------------------
# メモリ
# -----------------------------------------------------------------------------
# MLOCK=false                     # RAM 側のモデルをスワップさせたくない場合は true
# CTX_SIZE=0
# BATCH_SIZE=2048
# UBATCH_SIZE=512
# CACHE_TYPE_K="f16"              # q8_0 にすると KV キャッシュの VRAM 使用量を半減できる
# CACHE_TYPE_V="f16"

# -----------------------------------------------------------------------------
# 並列・スループット
# -----------------------------------------------------------------------------
# PARALLEL=-1
# CONT_BATCHING=true
# KV_UNIFIED=true

# -----------------------------------------------------------------------------
# サーバー
# -----------------------------------------------------------------------------
HOST="0.0.0.0"
PORT=8080
# API_KEY=""
# TIMEOUT=600
# THREADS_HTTP=-1

# -----------------------------------------------------------------------------
# 思考モード
# -----------------------------------------------------------------------------
# REASONING_FORMAT="auto"
# REASONING_BUDGET=-1

# -----------------------------------------------------------------------------
# プロセス優先度
# -----------------------------------------------------------------------------
# PRIO=0

# -----------------------------------------------------------------------------
# ログ
# -----------------------------------------------------------------------------
# LOG_VERBOSITY=3
